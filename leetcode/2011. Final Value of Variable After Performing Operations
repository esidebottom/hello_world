class Solution:
    def finalValueAfterOperations(self, operations: List[str]) -> int:
        total=0
        for ops in operations:
            if "-" in ops:
                total -= 1
            else:
                total += 1
        return(total)
