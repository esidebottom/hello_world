class Solution:
    def smallestNumber(self, n: int) -> int:
        set_bits=[1,3]
        for x in range(2,10):
            set_bits.append(2**x+set_bits[x-1])
        for x in range(10):
            if n-set_bits[x]<=0:
                return(set_bits[x])
        
        