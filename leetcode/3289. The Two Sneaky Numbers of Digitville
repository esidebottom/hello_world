class Solution:
    def getSneakyNumbers(self, nums: List[int]) -> List[int]:
        d={}
        a=[]
        for x in nums:
            if x in d:
                a.append(x)
            else:
                d[x]=1
        return(a)

